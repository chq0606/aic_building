# AIC Building 使用说明

> 面向演示者 / 评委的快速上手文档。开发者细节见 [README.md](README.md)。

## 一、启动方式

平台 5 个进程, 两条启动路径 **二选一**:

| 进程 | 作用 | 必需性 |
|------|------|--------|
| **PostgreSQL** | 数据库 (PG 15 + pgvector) | 必需 |
| **backend** | FastAPI 后端 API (port 8000), 内置异步跑 PDF 解析 / embedding | 必需 |
| **frontend** | Vue3 前端 dev server (port 5173) 或 nginx (port 8080) | 必需 |
| **prediction_worker** | 能耗预测 worker (Prophet / LSTM / Linear, CPU) | 跑预测才需要 |
| **triposplat_worker** | 单图 3D 重建 worker (TripoSplat, 需 NVIDIA GPU + 3.6GB 权重) | 跑 3D 重建才需要 |

> 后端是 IO 密集 (HTTP / DB), worker 是 CPU/GPU 密集 (训练 60s / 推理 2 分钟)。塞进同一进程会卡死后端 API, 所以 worker 独立进程, 挂了不拖累后端。Docker 方式 A 用 compose 自动起所有容器, 本地方式 B 需要手动开终端。

---

### 方式 A: Docker 一键启动 (推荐, 不用装 conda)

前置: 装 Docker Desktop。GPU 重建功能额外需要 NVIDIA 驱动 + nvidia-container-toolkit (Linux) 或 Docker Desktop GPU 支持 (Windows)。

#### A.1 启动步骤

```bash
cd e:/vscode_python/aic_building

# 1. 复制环境变量样例
cp .env.example .env

# 2. (可选) 编辑 .env, 填 GLM_API_KEY (AI 抽屉用, 留空则 AI 抽屉返诊断错误)
#    不改也能跑, demo 流程除 AI 抽屉外都通

# 3. 启动服务 (二选一)
make up          # CPU 模式: 4 容器 (postgres + backend + prediction_worker + frontend), 不含 3D 重建
make gpu-up      # GPU 模式: 5 容器 (上面 4 个 + triposplat_worker), 额外跑 3D 重建

# 4. (仅 GPU 模式首次) 下 TripoSplat 权重 3.6GB
make download-ckpts

# 5. 灌 demo 数据
make seed-demo

# 6. 浏览器打开 http://localhost:8080, 登录 demo / demo123
```

首次启动 5-15 分钟 (拉镜像 + 装 Python 依赖 + 下 BGE 模型 ~1.3GB)。第二次启动 30 秒内。

#### A.2 各 make 命令做什么

| 命令 | 作用 | 耗时 | 产物 |
|------|------|------|------|
| `make up` | 起 4 个 CPU 容器: postgres (PG15+pgvector) + backend (FastAPI) + prediction_worker (Prophet) + frontend (nginx) | 首次 5-15 分钟, 二次 30 秒 | 4 个容器跑起来, http://localhost:8080 可访问 |
| `make gpu-up` | 在 `make up` 基础上多起 triposplat_worker 容器 (GPU, 走 Dockerfile.triposplat) | 额外 2-5 分钟构建 GPU 镜像 | 5 个容器, 3D 重建 job 能被消费 |
| `make download-ckpts` | 下 TripoSplat 5 个权重文件共 3.6GB 到 `code/backend/storage/triposplat/ckpts/` | 看网速, 10-30 分钟 | 权重文件, worker 启动时校验 |
| `make seed-demo` | 跑 `code/seed_bdg2.py` 灌 demo 数据: 1 tenant + 1 site + 6 buildings + ~17 万条 hourly 读数 + 天气数据 | 30-60 秒 | demo 账号登录后能看到 6 栋楼 |
| `make down` | 关闭所有容器, 数据保留 | 10 秒 | 容器停, 数据卷保留 |
| `make clean` | 关闭 + 删数据卷 (数据库清空) | 10 秒 | 数据全丢, 谨慎 |
| `make logs` | 实时日志所有服务 | - | Ctrl+C 退出 |
| `make logs-svc=backend` | 只看指定服务 (svc 可换 prediction_worker / frontend / postgres / triposplat_worker) | - | - |
| `make ps` | 看容器状态 | - | - |

#### A.3 demo 数据说明

`make seed-demo` 灌入的数据 (来自 BDG2 公开数据集):

- **1 个租户**: demo 账号所属租户
- **1 个园区**: BDG2 园区, 含 6 栋楼 (Dylan / Emma / Olivia / Noah / Ava / Liam)
- **6 栋楼**: 每栋有完整元数据 (面积 / 层数 / 用途 / 建成年限) + ~29000 条 hourly 能耗读数 (1 年)
- **天气数据**: 园区所在城市 1 年 hourly 气温 / 湿度
- **demo 账号**: demo / demo123, 只读 + 大部分写 (源数据写操作被拦, 见第三节)

灌入后 demo 登录就能看到: 园区页 6 栋楼 3D 体块按能耗着色 / 分析中心 7 个图表都有数据 / 异常检测有历史事件 / 预测有 1 年历史数据可训练。

#### A.4 GLM API Key 配置 (AI 抽屉用)

AI 抽屉调智谱 GLM-4.5, 需要自己提供 Key。Docker 模式两条路:

**路 1: .env 全局配 (所有用户共用)**
```bash
# 编辑 .env
GLM_API_KEY=xxx.xxxxxx   # 智谱开放平台申请: https://open.bigmodel.cn/

# 重启 backend 让 .env 生效
make down && make up
```

**路 2: 设置页用户级配 (推荐, 每用户独立)**
1. demo / demo123 登录后进 `/settings` 设置页
2. 左侧选 "API Key" -> "GLM API Key" 输入框粘贴 Key
3. 点 "测试连接" (发 ping 到智谱, 1-3 秒, 绿色对勾 = 通)
4. 点 "保存" (Key AES-GCM 加密入库, 用户级, 优先级高于 .env 全局)

用户级 Key 存数据库, 容器重启不丢。.env 全局 Key 容器重启在, `make clean` 才丢。

#### A.5 demo 登录后完整体验流程

Docker 模式 (make up + make seed-demo 后) 跟本地模式体验一致, demo 账号登录后:

1. **园区页** (`/park`): Cesium 3D 地图, 6 栋楼 demo 数据按 EUI / 总能耗 / 异常数着色, 点楼栋卡片看详情抽屉
2. **分析中心** (`/analysis`): 7 个 ECharts 图表 (能耗排名 / 时序对比 / EUI 基准 / 异常汇总 / 能耗-天气关联 / 数据质量 / 预测)
3. **数据接入** (`/data-hub`):
   - 数据上传 tab: demo 不能上传 CSV (源数据写被拦), 但能看 demo 数据已灌入的 session 列表
   - 3D 建模 tab: demo 能上传建筑照片 + 提交 3D 重建 job (需 GPU 模式 + triposplat_worker 起着)
   - 知识库 tab: demo 能上传 PDF (PyMuPDF 解析 + BGE embedding), 默认知识库是空的
4. **AI 抽屉** (右下角浮动按钮): 配 GLM Key 后能问答, 支持 function call 自动调查询/检索工具
5. **设置页** (`/settings`): 账号 / API Key / 数据源 / 关于

3D 重建体验完整链路 (需 `make gpu-up` + `make download-ckpts`):
1. 园区页点楼栋 -> 详情抽屉 -> "3D 重建"
2. 上传建筑正脸照片 + 填尺寸 (length/width/height/floors)
3. 提交后拿 job_id, 列表 5s 自动刷新状态
4. PENDING -> RUNNING (worker 拉到, ~2 分钟推理) -> SUCCEEDED
5. 点"查看预览"看 3D 模型, SUCCEEDED 后回园区页能看到 3D 高斯泼溅渲染

#### A.6 seed-demo 潜在问题

`make seed-demo` 跑 `docker compose exec backend python code/seed_bdg2.py`, 但 `seed_bdg2.py` 在项目根 `code/` 下, backend 容器 `COPY code/backend/ /app/` 没把它带进去, docker-compose.yml 也没挂 `code/` 目录到 backend 容器。**可能报 `No such file or directory`**。

如果报错, 临时用本地方式灌:
```bash
# 起 docker PG (如果没起)
docker compose up -d postgres

# 本机 aic env 跑 seed (连 docker PG)
cd e:/vscode_python/aic_building/code/backend
e:/anaconda/envs/aic/python.exe ../seed_bdg2.py
```

这个 bug 后续修 (加 volume 挂载或改 Makefile 走本机 python)。

---

### 方式 B: 本地直接跑 (开发用, 需要装 conda)

前置: 装 conda + PostgreSQL 15 + pgvector (或单独 `docker compose up -d postgres` 用容器 PG)。

**aic env 创建 (首次)**:

```bash
# 从 building_aic clone (保留 SAC 绕开源生栈: numpy 1.26.4 / matplotlib 3.7.5 / Pillow 9.5.0 / transformers 4.44.2)
conda create --name aic --clone building_aic

# 卸 CPU torch, 装 GPU 版 (cu130)
e:/anaconda/envs/aic/python.exe -m pip uninstall -y torch torchvision
e:/anaconda/envs/aic/python.exe -m pip install torch==2.12.1+cu130 torchvision==0.27.1+cu130 --index-url https://download.pytorch.org/whl/cu130

# 验证 GPU 可用 (应输出 True)
e:/anaconda/envs/aic/python.exe -c "import torch; print(torch.cuda.is_available())"
```

> torch 必须装 **2.12.1+cu130**, 不要装 2.13.0+cu130。Windows 11 Home SAC 会拦 2.13.0 (云信誉未命中), 2.12.1 已验证过 SAC。详见"方式 B 补充: GPU 环境自查"。

**最少 2 个终端** (PostgreSQL 当 Windows 服务或 docker 容器后台跑, 不占终端):

```bash
# ─── 终端 1: 后端 (必需) ─────────────────────────────────────
conda activate aic
cd e:/vscode_python/aic_building/code/backend
python -m uvicorn app.main:app --reload
# 注意: 数据库连接串从 code/backend/.env 读, 不要 $env:DATABASE_URL=... (pydantic-settings OS env > .env, 会盖掉 .env 正确密码导致 500)
# → http://localhost:8000

# ─── 终端 2: 前端 (必需) ─────────────────────────────────────
cd e:/vscode_python/aic_building/code/frontend
node_modules/.bin/vite
# → http://localhost:5173
```

以上 2 个终端就能跑: 园区页 / 分析中心 / 知识库 / AI 抽屉 / 设置页。**提交预测 job 或 3D 重建 job 才需要再开下面两个**:

```bash
# ─── 终端 3: 能耗预测 worker (跑预测才需要) ─────────────────
# 不起则提交预测 job 后一直 PENDING, 前端卡在"预测中..."
conda activate aic
cd e:/vscode_python/aic_building/code/backend
python -m worker.prediction_worker

# ─── 终端 4: 3D 重建 worker (跑单图重建才需要, 需 GPU) ──────
# 前置 (只跑一次): make download-ckpts  下 3.6GB TripoSplat 权重
# 不起则提交重建 job 后一直 PENDING, 其他功能不受影响
conda activate aic
cd e:/vscode_python/aic_building/code/backend
python -m worker.triposplat_worker
# 启动花 ~10s 加载 3.6GB 权重, 看到 "pipeline 就绪, 进入主循环" 才会消费 job
# 推理单 job 约 2 分钟, 生成 .ply + preview.png + 3D Tiles (tileset.json + tiles/*.glb)
```

---

### 方式 B 补充: GPU 环境自查 (跑 3D 重建前必看)

跑 3D 重建 (TripoSplat) 必须 NVIDIA GPU + 正确版本的 torch / torchvision。装错版本 worker 启动正常、能拉到 job, 但跑到 TripoSplat 内部 deform_conv2d 时崩, 报 `NotImplementedError: Could not run 'torchvision::deform_conv2d' with arguments from the 'CUDA' backend`, 自动重试 3 次后 job 置 FAILED。

**自查命令** (aic env 里):

```bash
python -c "import torch, torchvision; print('torch:', torch.__version__); print('torchvision:', torchvision.__version__); print('cuda available:', torch.cuda.is_available())"
```

正确输出:

```
torch: 2.12.1+cu130
torchvision: 0.27.1+cu130
cuda available: True
```

关键看后缀: torch 和 torchvision **都要 +cu130** (CUDA 13.0 版)。`cuda available: True` 不代表 torchvision ops 能跑, 必须后缀对。

**SAC 拦截 (Windows 11 Home China)**: 本机 Smart App Control (SAC) Enforce 模式会拦"未签名 + 不在云信誉库"的 .pyd/.dll。torch 2.13.0+cu130 被 SAC 拦 (c10.dll 云信誉未命中), 装包后 import 报 `DLL load failed ... 应用程序控制策略已阻止此文件`。torch 2.12.1+cu130 已进云信誉库, 不拦。**同包名不同小版本, SAC 命中结果不同, 必须精确装 2.12.1, 不要装 2.13.0**。matplotlib 3.11.1 / Pillow 12.3.0 / transformers 5.14.1 同理被拦, aic env 保留的 matplotlib 3.7.5 / Pillow 9.5.0 / transformers 4.44.2 是已验证过 SAC 的老版本, 不要升级。SAC 没有手动白名单, 关了不可逆, 治本就是锁版本号。

**典型坑**: `pip install torch` 默认从 PyTorch 索引装 CUDA 版, 但 `pip install torchvision` 不指定 `--index-url` 时 PyPI 默认装 CPU 版 (后缀 +cpu)。两者版本号看似匹配 (0.27.1 配 2.12.1), 后缀不同就崩 - torch 能跑 CUDA, 但 torchvision 的 ops (deform_conv2d / nms / roi_align) 没编译进 CUDA 后端, 在 GPU tensor 上调直接 NotImplementedError。

**装错修复**:

```bash
pip uninstall torchvision -y
# 清理 ~orchvision 残留 (uninstall 偶尔留临时目录, 不清会污染新装, import 时报 "operator torchvision::nms does not exist")
rm -rf e:/anaconda/envs/aic/Lib/site-packages/~orchvision
pip install torchvision --index-url https://download.pytorch.org/whl/cu130
```

**验证** deform_conv2d 真的能在 GPU 上跑:

```bash
python -c "import torch, torchvision; x = torch.randn(1, 4, 8, 8, device='cuda'); w = torch.randn(2, 4, 3, 3, device='cuda'); offset = torch.zeros(1, 18, 8, 8, device='cuda'); mask = torch.ones(1, 9, 8, 8, device='cuda'); bias = torch.zeros(2, device='cuda'); out = torchvision.ops.deform_conv2d(input=x, weight=w, offset=offset, mask=mask, bias=bias, padding=(1,1)); print('deform_conv2d on CUDA OK:', out.shape)"
```

输出 `deform_conv2d on CUDA OK: torch.Size([1, 2, 8, 8])` 才算彻底好。

**显存要求**: TripoSplat 推理峰值 ~4.6GB 显存。RTX 5060 (8GB) 够用, 4GB 卡可能 OOM。`nvidia-smi` 看显存占用, worker 启动后空闲时应稳定在 3.6GB 左右 (权重常驻), 推理时涨到 4.6GB。

**权重文件**: `make download-ckpts` 下 5 个文件共 3.6GB 到 `code/backend/checkpoints/triposplat/`。worker 启动时 `_check_ckpts` 会校验 5 个文件都存在, 缺一个直接报 FileNotFoundError。下的位置不对改 `.env` 的 `TRIPOSPLAT_CKPTS_PATH`。

## 二、登录账号

平台内置两类账号:

| 账号 | 密码 | 权限 | 用途 |
|------|------|------|------|
| `demo` | `demo123` | 读 + 大部分写 (只拦源数据写, 见第三节) | 演示 / 评委验证 |
| 自行注册 | 自设 | 全功能 (读 + 写) | 完整功能体验 |

注册入口: 登录页点"立即注册", 填邮箱 + 密码即可。注册后自动建独立租户, 看到的是空数据 (没有 demo 数据), 需自己上传 CSV / PDF。

## 三、demo 账号功能边界

demo 账号设计目标是"评委无需注册即可体验完整 demo 流程", 所以权限策略是 **只拦源数据写操作** (会改能耗 fact 表 / staging 表的接口), 其他写操作 (知识库 PDF / 3D 重建 / 体块 / 异常检测 / 预测 job) demo 都能调, 让评委能完整走一遍分析 - 重建 - 预测 - 问答的链路。

### demo 能做的 (读 + 大部分写)

- 园区页 (`/park`): 6 栋楼 demo 数据 + Cesium 3D 体块着色 (按 EUI / 总能耗 / 异常数分档)
- 分析中心 (`/analysis`): 7 个 ECharts 图表 (建筑能耗排名 / 多楼时序对比 / EUI 基准对比 / 异常事件汇总 / 能耗-天气关联 / 数据质量面板 / 能耗预测)
- 数据质量 / 异常列表 / 能耗预测结果查看
- 知识库全功能 (上传 PDF / 触发解析 + embedding / 列表 / 检索) - 知识库不属源能耗数据, demo 也能用
- AI 抽屉问答 (需先配 GLM Key, 见下节)
- 配置自己的 GLM API Key (`PUT /settings/glm-api-key`, AES-GCM 加密入库, 用户级)
- BGE 模型状态查看 + 测试 embedding
- 数据源状态查看
- 上传建筑照片 (`POST /uploads/photo`, demo 可用) + 提交单图 3D 重建 job (`POST /buildings/{id}/reconstruction/single-photo`, demo 可用)
- 提交体块模型参数 (`POST /buildings/{id}/visual-models/block`, demo 可用)
- 触发异常检测 (`POST /anomalies/detect`, demo 可用) - 异常检测只写事件表, 不改源能耗
- 提交能耗预测 job (`POST /prediction/buildings/{id}/forecast`, demo 可用) - 预测只写 mart.prediction_job, 不改 fact

### demo 不能做的 (源数据写操作, 全部返 403)

- 上传能耗 CSV / XLSX (`POST /uploads/single` / `/multi` - 走 `require_write_access` 拦 demo)
- 触发 import run / rollback (`POST /imports/{batch_id}/run` / `/rollback` - 写 fact 表)
- 重新 seed demo 数据 (`POST /admin/seed-demo`)
- 修改 demo 账号密码 (`PUT /auth/me` - 路由层显式拦 demo, 避免改了密码破坏 demo 体验账号)

其他用户 (自行注册) 拥有完整读写权限, 看到的是空数据 (没有 demo 数据), 需自己上传 CSV / PDF。

## 四、AI 抽屉使用步骤 (demo 也能用)

AI 抽屉调智谱 GLM-4.5 模型, 需要自己提供 API Key。所有用户 (包括 demo) 都能配自己的 Key, 加密存数据库, 按用户隔离。

### 4.1 申请 GLM API Key

1. 访问智谱开放平台 `https://open.bigmodel.cn/`
2. 注册账号 -> 实名认证 -> 创建 API Key
3. 复制 Key (形如 `xxx.xxxxxx`)

### 4.2 配置 Key

1. demo / demo123 登录
2. 顶部菜单进"设置"页 (`/settings`)
3. 左侧菜单选 "API Key"
4. 在 "GLM API Key" 输入框粘贴 Key
5. 点 "测试连接" (会发一条 ping 到智谱, 1-3 秒, 成功显示绿色对勾)
6. 测试通过后点 "保存" (Key 加密入库, 之后该用户再用 AI 抽屉不用重输)

### 4.3 用 AI 抽屉

1. 任意页面右下角浮动按钮 (橙色圆按钮), 点开抽屉
2. 抽屉里能选会话上下文 (当前建筑 / 当前页面)
3. 输入问题回车发送, GLM 流式返回 (SSE 实时打字)
4. 抽屉支持 function call, 可自动调查询 / 检索工具, 引用知识库条款
5. 历史会话在左侧会话列表, 可切换

## 五、知识库现状

### 默认状态

平台 `test_pdfs/` 目录里有 20 个国标 / 行业 / 地方标准 PDF (GB 55015-2021 / GB 50736-2012 / GB 19577-2024 等), 但 **seed 流程不自动导入这些 PDF**。

也就是说: demo 登录后默认知识库是 **空的**, 检索任何国标条款都返空结果, AI 抽屉问国标条款也答不上来。demo 账号可以自己在 `/data-hub` 知识库 tab 上传 PDF 体验完整流程 (PyMuPDF 解析约 5-15s/份, BGE embedding 约 30-60s/份)。

### 预灌入知识库 (可选, 展示前用)

如果想演示时知识库默认就有内容, 二选一:

| 方案 | 做法 | 代价 |
|------|------|------|
| A (推荐) | 展示前用 admin 账号或 demo 账号手动在 `/data-hub` 知识库 tab 上传几个核心国标 PDF (GB 55015-2021 / GB 50736-2012 等) | 0 代码, 上传后约 1-2 分钟解析 + embedding 完成 |
| B | 写 `seed_pdfs.py` 脚本, `make seed-demo` 时自动灌几个核心国标进 demo 知识库 | 1 个新脚本 + Makefile 加一行 + 首次跑要等 OCR + embedding, 约 5-10 分钟 |

## 六、各页面功能速览

### `/park` 园区页 (首页)

- Cesium 3D 地图, 6 栋楼 demo 数据按能耗着色
- 顶部时间范围切换 / 指标切换 (总能耗 / EUI / 异常数)
- 左侧园区 + 楼栋列表
- 点楼栋卡片打开详情抽屉 (能耗 / 异常 / 元数据)
- 右下角全局 AI 抽屉按钮

### `/analysis` 分析中心

7 个 ECharts 图表 (2x4 网格, 预测面板跨 2 列):
1. 建筑能耗排名 (横向条形, 按 total_kwh / eui / anomaly_count 切换)
2. 多楼时序对比 (多楼折线叠加, 切日 / 周 / 月粒度)
3. EUI 基准对比 (楼栋 EUI vs GB 55015-2021 强制限值 80 kWh/m²)
4. 异常事件汇总 (按 severity 堆叠 + 按事件类型分组)
5. 能耗-天气关联 (能耗 vs 温度散点 + 相关系数)
6. 数据质量面板 (完整性 / 及时性 / 异常率三档堆叠条形)
7. 能耗预测 (Prophet / LSTM / Linear 三模型对比 + MAPE + 未来 7/14/30 天预测)

### `/data-hub` 数据接入

3 个 tab (左侧切):

**数据上传 tab**
- 模板下载 (CSV 模板 + 字段说明)
- 文件上传 (拖拽 CSV / XLSX, 进 4 步列映射向导: 列名 -> 时间格式 -> 单位 -> 校验提交)
- 上传 session 列表 + import batch 列表 (含状态轮询)

**3D 建模 tab**
- 体块模型表单 (手填 length/width/height/floors, demo 也能提交)
- 单图 3D 重建 (上传建筑正脸照片 + 填尺寸, 提交后异步 worker 跑 TripoSplat, 需 GPU)
- 重建任务列表 (PENDING / RUNNING / SUCCEEDED / FAILED 状态轮询 + preview 预览)

3D 重建使用要点:

**照片要求**: 建筑正脸照, 尽量无遮挡 / 无透视畸变;.jpg / .png / .webp, 最大 10MB; 分辨率 512×512 以上, 太低 TripoSplat 重建细节糊。

**尺寸建议** (length_m / width_m / height_m / floors_count):
- floors_count 照实际填 (如 Dylan = 2)
- height_m 不要照实际填 3.5m × floors。项目前端有 Z_VISUAL_SCALE=1.8 + computeVisualHeight 保底机制, 渲染高度 = max(height_m × 1.8, 20 + floors × 4)。floors=2 时保底 28m, 想让输入的 height_m 真正生效需 > 15.6m
- 推荐 height_m = 20-28, 渲染后 36-50m, 看起来像 5-7 层真实建筑 (BDG2 真实层高 3.5m 太扁, 这就是项目设计 Z_VISUAL_SCALE 的初衷)
- length / width 按真实 footprint 适当缩放, 太大占地图, 太小看不见。以 Dylan (sqm=17358, 真实 footprint ~8679 sqm) 为例, 推荐 length=80, width=55, height=24, floors=2, 渲染后 80×55×43.2m

**提交后流程**: 提交即拿 job_id, 进 PENDING。worker 每 5s 轮询拉 PENDING job, 拉到后置 RUNNING 跑 TripoSplat 推理约 2 分钟。前端列表 5s 自动刷新状态, PENDING/RUNNING 时显示进度条 + 已等待时长。SUCCEEDED 后点"查看预览"弹 ModelPreview 看 3D 模型。FAILED 后点"重试"按钮 (retry_count 重置为 0, 重新进 PENDING)。

**知识库 tab**
- PDF 上传 (PyMuPDF + EasyOCR 解析 + BGE embedding, demo 也能用)
- 文档列表 (状态: pending / parsing / parsed / embedding / ready / failed)
- 触发重新解析 / 重新 embedding / 删除

### `/settings` 系统设置

4 个左侧菜单:
- 账号 (邮箱 / 显示名 / 改密码; demo 账号改密码被拦)
- API Key (GLM Key 配置 + 测试连接; BGE 模型状态 + 测试 embedding, 见第四节)
- 数据源 (租户数据量统计: building/point/reading/anomaly_count + 最近一次 seed 时间)
- 关于 (版本 / 技术栈清单 / 源码仓库链接)

### AI 抽屉 (右下角浮动按钮)

- 智谱 GLM-4.5 流式响应 (SSE 实时打字)
- 会话管理 (新建 / 切换 / 历史)
- 上下文感知 (自动带入当前园区 / 楼栋 / 页面)
- function call 工具集 (查能耗 / 检索知识库 / 找异常 / 查天气)
- 节能优化方案结构化输出 (问题 / 措施 / 收益 / 优先级四段卡片)
- 一键导出节能方案 PDF (基于结构化方案生成)

## 七、常见报错

### Q: 启动后访问 http://localhost:8080 显示 502 / 连不上

A: backend 还在启动, 等 30 秒重试。`make logs` 看后端日志, 出现 `Uvicorn running on http://0.0.0.0:8000` 才算就绪。

### Q: demo / demo123 登录提示"用户名或密码错误"

A: `make seed-demo` 没跑或失败了, demo 用户没建。`make logs-svc=backend` 看错误, 通常是 postgres 还没 healthy 就执行了 seed, 重跑一次 `make seed-demo` 即可。

### Q: AI 抽屉发消息提示 "GLM_API_KEY 未配置"

A: 没在设置页配 Key, 或 .env 里 `GLM_API_KEY` 没填 + 该用户也没在设置页存自己的 Key。二选一即可:
- 后端 `.env` 填 `GLM_API_KEY=xxx` (全局默认 Key, 所有用户共用)
- 或用户在设置页存自己的 Key (用户级, 优先级高于全局)

### Q: 知识库检索返空 `chunks: []`

A: 知识库默认空, 见第五节。demo 账号可自己在 `/data-hub` 知识库 tab 上传 PDF。

### Q: BGE 模型首次加载慢

A: 模型 ~1.3GB, 首次启动从 HuggingFace 下载。国内网络慢可设 `HF_ENDPOINT=https://hf-mirror.com` 走镜像。下载完缓存到 `~/.cache/huggingface/`, 第二次秒级加载。

### Q: 提交预测 job 一直 PENDING

A: prediction_worker 没起来。Docker 方式 `make ps` 看 `prediction_worker` 容器状态, `make logs-svc=prediction_worker` 看错误; 本地方式 B 参"方式 B 补充"那节, 需单独开终端跑 `python -m worker.prediction_worker`。常见原因: 缺 torch 或 prophet 依赖。

### Q: 上传建筑照片报 500 服务器内部错误

A: 后端 `app/api/upload.py` 历史版本漏了 `from app.core.config import settings`, 校验照片大小调 `settings.photo_max_bytes` 时抛 NameError -> 500。已修复, **需重启 uvicorn 让修复生效** (方式 B 直接 Ctrl+C 再 `python -m uvicorn app.main:app --reload`; 方式 A `docker compose restart backend`)。重启后再上传即正常, 上传成功会在照片区显示绿色"已上传"标签。

注意: 上传照片接口 (`POST /uploads/photo`) demo 账号也能调, 不属于源数据写操作, 不挂 `require_write_access`。如果 demo 调上传照片返 403, 说明后端版本旧, 同步重启即可。

### Q: 提交 3D 重建 job 后一直 PENDING, 不进 RUNNING

A: triposplat_worker 没启动。worker 是独立进程, 默认后端 `uvicorn` 不带它。方式 B 参"方式 B 补充"那节新开终端 `python -m worker.triposplat_worker`; 方式 A 用 `make gpu-up` 而不是 `make up`。worker 启动花 ~10s 加载 3.6GB 权重, 日志看到 `pipeline 就绪, 进入主循环` 才会消费 job。worker 跑起来后, PENDING 的 job 会自动被拉起, 不用重新提交。

前端有兜底提示: PENDING job 超 30s 没进 RUNNING, 列表顶部会弹黄色 Alert "TripoSplat worker 可能未启动", 直接给启动命令。

### Q: 3D 重建 job 进 RUNNING 后很快 FAILED, 日志报 `torchvision::deform_conv2d CUDA backend` 错

A: torchvision 装了 CPU 版 (+cpu 后缀), torch 是 CUDA 版 (+cu130)。TripoSplat 内部 deform_conv2d 在 GPU tensor 上调, 但 torchvision CUDA ops 没编译进去, 报 NotImplementedError。worker 自动重试 3 次都失败置 FAILED。

排查: `python -c "import torch, torchvision; print(torch.__version__, torchvision.__version__)"` 看后缀。torchvision 应该是 `0.27.1+cu130` 不是 `0.27.1+cpu`。

修复: 见"方式 B 补充: GPU 环境自查"那节, 重装 torchvision CUDA 版 + 清 `~orchvision` 残留目录。装完用文档里的 deform_conv2d 验证命令确认能跑。

修好后必须**手动点列表里 FAILED job 的"重试"按钮**, retry_count 重置为 0, 重新进 PENDING, 这次 worker 能正常消费。

### Q: 3D 重建 job 显示 SUCCESS 但 /park 没看到 3D 模型

A: 三种可能: (1) 前端缓存 - 强制刷新 (Ctrl+Shift+R); (2) worker 生成了 .ply 但转 3D Tiles 失败 (node 没装或 3dgs-ply-3dtiles-converter 没下), 这种情况前端降级到静态点云渲染, 仍能看到模型只是没 LOD; (3) building_visual_model 表里 `tiles_path` 字段为 NULL, 检查 worker 日志有没有 `_convert_ply_to_3dtiles` 报错。

### Q: 3D 重建 job FAILED 后, 修了 worker 怎么重新跑

A: 列表里 FAILED 状态的 job 有"重试"按钮, 点了 retry_count 重置为 0, error_message 清空, 状态回 PENDING, worker 下一轮 (5s 内) 就会拉起。不需要重新上传照片或填表单。如果不想保留失败记录, 也能点"删除"按钮硬删 job + 磁盘产物 + 关联 visual_model 记录, 重新提交一个新的。

### Q: Windows 上 `make up` 很慢 / postgres 容器卡

A: NTFS 直挂数据卷性能差 10 倍。建议用 WSL2 + Docker Desktop, 把项目放在 WSL 文件系统里 (如 `\\wsl$\Ubuntu\home\<user>\aic_building`)。

## 八、关闭 / 重置

```bash
make down              # 关闭所有服务 (含 GPU 模式起的 triposplat_worker), 数据保留
make clean             # 关闭 + 删数据卷 (数据库清空, 谨慎)
make up                # 重新启动 CPU 模式 (4 服务, 不含 triposplat_worker)
make gpu-up            # 重新启动 GPU 模式 (5 服务, 含 triposplat_worker)
make seed-demo         # 重新灌 demo 数据
```

## 九、常用命令速查

| 命令 | 作用 |
|------|------|
| `make up` | 启动 4 服务 (CPU 模式, 不含 triposplat_worker) |
| `make gpu-up` | 启动 5 服务 (含 GPU triposplat_worker) |
| `make down` | 关闭 |
| `make seed-demo` | 灌 BDG2 demo 数据 (6 栋楼 / ~17 万条读数) |
| `make test` | 跑后端 pytest + 前端 vitest |
| `make test-backend` | 只跑后端测试 |
| `make test-frontend` | 只跑前端测试 |
| `make logs` | 实时日志 (所有服务) |
| `make logs-svc=backend` | 只看 backend 日志 (svc 参数可换 prediction_worker / frontend / postgres) |
| `make ps` | 服务状态 |
| `make rebuild` | 强制重建镜像 (改 Dockerfile 后) |
| `make download-ckpts` | 下 TripoSplat 权重 3.6GB (GPU 功能首次必跑) |
| `make clean` | 关闭 + 删数据卷 (谨慎) |

# 避免 build context 把大目录送进 docker daemon, 加速 docker compose build
# 不影响运行, 只影响构建时传给 docker 的文件集合

# Git
.git
.gitignore

# 文档 / 工作记录 (镜像里不需要)
*.md
*.txt
*.pdf
*.docx
*.pptx
工作记录_*.md
提示词*.txt
使用说明.md
README.md
# requirements.txt 是 pip install 依赖清单, 必须进镜像, 上面 *.txt 会误伤, 取反恢复
!code/backend/requirements.txt

# 环境配置 (含数据库密码 / API key, 不进镜像, 由 docker-compose environment 注入)
.env
**/.env

# Python 缓存 + 本地虚拟环境
**/__pycache__/
**/*.pyc
**/*.pyo
.venv/
venv/

# 前端 node_modules (容器内 pnpm install 重装, 不复用本机的)
code/frontend/node_modules/
code/frontend/dist/

# 后端运行时产物 (容器内自己生成)
code/backend/logs/
code/backend/data/
code/backend/storage/
code/backend/__pycache__/

# 数据集 (extracted_data 单独挂载为只读 volume, 不进镜像)
extracted_data/

# Docker 自身文件 (避免循环)
docker-compose*.yml
Dockerfile*

# 测试 PDF / 临时脚本
aic_building/test_pdfs/
**/_test_*.py
**/_check_*.py
**/_diag_*.py

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
